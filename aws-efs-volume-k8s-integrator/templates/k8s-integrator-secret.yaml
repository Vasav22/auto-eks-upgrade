apiVersion: v1
kind: Secret
type: kubernetes.io/service-account-token
metadata:
  name: {{ include "k8s-integrator-volume.fullname" . }}
  namespace: {{ .Values.k8sIntegratorNamespace }}
  annotations:
    kubernetes.io/service-account.name: {{ include "k8s-integrator-volume.fullname" . }}
