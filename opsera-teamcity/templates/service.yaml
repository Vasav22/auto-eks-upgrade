apiVersion: v1
kind: Service
metadata:
  name: {{ include "opsera-teamcity.fullname" . }}
  labels:
    {{- include "opsera-teamcity.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opsera-teamcity.selectorLabels" . | nindent 4 }}
