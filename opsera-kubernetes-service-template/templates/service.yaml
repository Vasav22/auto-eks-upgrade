apiVersion: v1
kind: Service
metadata:
  name: {{ include "opsera-kubernetes-service-template.fullname" . }}
  labels:
    {{- include "opsera-kubernetes-service-template.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opsera-kubernetes-service-template.selectorLabels" . | nindent 4 }}
