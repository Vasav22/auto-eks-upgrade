apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "opsera-GitlabCi.fullname" . }}-test-connection"
  labels:
    {{- include "opsera-GitlabCi.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "opsera-GitlabCi.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
