apiVersion: v1
kind: Service
metadata:
  name: {{ include "opsera-GitlabCi.fullname" . }}
  labels:
    {{- include "opsera-GitlabCi.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opsera-GitlabCi.selectorLabels" . | nindent 4 }}
