apiVersion: v1
kind: Service
metadata:
  name: {{ include "opsera-ansible.fullname" . }}
  labels:
    {{- include "opsera-ansible.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opsera-ansible.selectorLabels" . | nindent 4 }}
