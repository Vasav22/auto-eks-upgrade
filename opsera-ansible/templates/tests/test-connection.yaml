apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "opsera-ansible.fullname" . }}-test-connection"
  labels:
    {{- include "opsera-ansible.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "opsera-ansible.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
