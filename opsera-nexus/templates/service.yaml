apiVersion: v1
kind: Service
metadata:
  name: {{ include "opsera-nexus.fullname" . }}
  labels:
    {{- include "opsera-nexus.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opsera-nexus.selectorLabels" . | nindent 4 }}
