apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "opsera-redis.fullname" . }}-test-connection"
  labels:
    {{- include "opsera-redis.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "opsera-redis.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
