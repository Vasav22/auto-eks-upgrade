apiVersion: v1
kind: Service
metadata:
  name: {{ include "opsera-redis.fullname" . }}
  labels:
    {{- include "opsera-redis.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opsera-redis.selectorLabels" . | nindent 4 }}
