apiVersion: v1
kind: Service
metadata:
  name: {{ include "opsera-jobengine-core.fullname" . }}
  labels:
    {{- include "opsera-jobengine-core.labels" . | nindent 4 }}
  annotations:
    {{- include "opsera-jobengine-core.annotations" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opsera-jobengine-core.selectorLabels" . | nindent 4 }}
