apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "opsera-letsencrypt.fullname" . }}-test-connection"
  labels:
    {{- include "opsera-letsencrypt.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "opsera-letsencrypt.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
