# Test
# helm upgrade --install tools-cleanup-cronjob . -n infra-cleanup --create-namespace -f values-test.yaml

# UAT
# helm upgrade --install tools-cleanup-cronjob . -n infra-cleanup --create-namespace -f values-uat.yaml

# ProdQA
# helm upgrade --install tools-cleanup-cronjob . -n infra-cleanup --create-namespace -f values-prodqa.yaml
