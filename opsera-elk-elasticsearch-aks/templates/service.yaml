apiVersion: v1
kind: Service
metadata:
  name: {{ include "opsera-elk-elasticsearch.fullname" . }}
  labels:
    {{- include "opsera-elk-elasticsearch.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: https
      protocol: TCP
      name: https
  selector:
    {{- include "opsera-elk-elasticsearch.selectorLabels" . | nindent 4 }}
