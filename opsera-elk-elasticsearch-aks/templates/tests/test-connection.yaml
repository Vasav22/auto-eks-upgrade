apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "opsera-elk-elasticsearch.fullname" . }}-test-connection"
  labels:
    {{- include "opsera-elk-elasticsearch.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "opsera-elk-elasticsearch.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
