resources:
  requests:
    memory: 512Mi

autoscaling:
  enabled: true
  minReplicas: 1
  maxReplicas: 2
  targetMemoryUtilizationPercentage: 240

ingress:
  enabled: true
  hosts:
    - host: user-management-service.opsera-dev-private.opsera.io

NODE_ENV: testing