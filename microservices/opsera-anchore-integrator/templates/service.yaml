apiVersion: v1
kind: Service
metadata:
  name: {{ include "opsera-anchore-integrator.fullname" . }}
  labels:
    {{- include "opsera-anchore-integrator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opsera-anchore-integrator.selectorLabels" . | nindent 4 }}
