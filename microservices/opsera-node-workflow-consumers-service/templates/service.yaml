apiVersion: v1
kind: Service
metadata:
  name: {{ include "opsera-node-workflow-consumers-service.fullname" . }}
  labels:
    {{- include "opsera-node-workflow-consumers-service.labels" . | nindent 4 }}
  annotations:
    {{- include "opsera-node-workflow-consumers-service.annotations" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opsera-node-workflow-consumers-service.selectorLabels" . | nindent 4 }}
