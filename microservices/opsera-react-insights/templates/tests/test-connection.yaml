apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "opsera-react-insights.fullname" . }}-test-connection"
  labels:
    {{- include "opsera-react-insights.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "opsera-react-insights.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
