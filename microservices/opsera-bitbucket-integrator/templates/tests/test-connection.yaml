apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "opsera-bitbucket-integrator.fullname" . }}-test-connection"
  labels:
    {{- include "opsera-bitbucket-integrator.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "opsera-bitbucket-integrator.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
