apiVersion: v1
kind: Service
metadata:
  name: {{ include "opsera-bitbucket-integrator.fullname" . }}
  labels:
    {{- include "opsera-bitbucket-integrator.labels" . | nindent 4 }}
  annotations:
    service.kubernetes.io/topology-mode: auto
    {{- include "opsera-bitbucket-integrator.annotations" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opsera-bitbucket-integrator.selectorLabels" . | nindent 4 }}
