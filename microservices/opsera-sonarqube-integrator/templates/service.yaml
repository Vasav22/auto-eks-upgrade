apiVersion: v1
kind: Service
metadata:
  name: {{ include "opsera-sonarqube-integrator.fullname" . }}
  labels:
    {{- include "opsera-sonarqube-integrator.labels" . | nindent 4 }}
  annotations:
    service.kubernetes.io/topology-mode: auto
    {{- include "opsera-sonarqube-integrator.annotations" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opsera-sonarqube-integrator.selectorLabels" . | nindent 4 }}
