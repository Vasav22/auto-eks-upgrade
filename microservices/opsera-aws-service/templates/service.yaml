apiVersion: v1
kind: Service
metadata:
  name: {{ include "opsera-aws-service.fullname" . }}
  labels:
    {{- include "opsera-aws-service.labels" . | nindent 4 }}
  annotations:
  {{- include "opsera-aws-service.annotations" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opsera-aws-service.selectorLabels" . | nindent 4 }}
