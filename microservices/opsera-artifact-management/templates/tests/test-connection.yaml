apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "opsera-artifact-management.fullname" . }}-test-connection"
  labels:
    {{- include "opsera-artifact-management.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "opsera-artifact-management.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
