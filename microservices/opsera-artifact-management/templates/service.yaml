apiVersion: v1
kind: Service
metadata:
  name: {{ include "opsera-artifact-management.fullname" . }}
  labels:
    {{- include "opsera-artifact-management.labels" . | nindent 4 }}
  annotations:
    {{- include "opsera-artifact-management.annotations" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opsera-artifact-management.selectorLabels" . | nindent 4 }}
