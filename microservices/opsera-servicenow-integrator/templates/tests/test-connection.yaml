apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "opsera-servicenow-integrator.fullname" . }}-test-connection"
  labels:
    {{- include "opsera-servicenow-integrator.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "opsera-servicenow-integrator.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
