apiVersion: v1
kind: Service
metadata:
  name: {{ include "opsera-ansible-integrator.fullname" . }}
  labels:
    {{- include "opsera-ansible-integrator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opsera-ansible-integrator.selectorLabels" . | nindent 4 }}
