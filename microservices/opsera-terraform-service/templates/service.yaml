apiVersion: v1
kind: Service
metadata:
  name: {{ include "opsera-terraform-service.fullname" . }}
  labels:
    {{- include "opsera-terraform-service.labels" . | nindent 4 }}
  annotations:
    service.kubernetes.io/topology-mode: auto
    {{- include "opsera-terraform-service.annotations" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opsera-terraform-service.selectorLabels" . | nindent 4 }}
