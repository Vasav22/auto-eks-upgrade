apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "opsera-terraform-service.fullname" . }}-test-connection"
  labels:
    {{- include "opsera-terraform-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "opsera-terraform-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
