apiVersion: v1
kind: Service
metadata:
  name: {{ include "opsera-gitscraper-analytics.fullname" . }}
  labels:
    {{- include "opsera-gitscraper-analytics.labels" . | nindent 4 }}
  annotations:
    {{- include "opsera-gitscraper-analytics.annotations" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opsera-gitscraper-analytics.selectorLabels" . | nindent 4 }}
