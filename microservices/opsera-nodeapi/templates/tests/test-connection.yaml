apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "opsera-nodeapi.fullname" . }}-test-connection"
  labels:
    {{- include "opsera-nodeapi.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "opsera-nodeapi.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
