apiVersion: v1
kind: Service
metadata:
  name: {{ include "opsera-nodeapi.fullname" . }}
  labels:
    {{- include "opsera-nodeapi.labels" . | nindent 4 }}
  annotations:
    service.kubernetes.io/topology-mode: auto
    {{- include "opsera-nodeapi.annotations" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opsera-nodeapi.selectorLabels" . | nindent 4 }}
