apiVersion: v1
kind: Service
metadata:
  name: {{ include "opsera-deployment-analytics-service.fullname" . }}
  labels:
    {{- include "opsera-deployment-analytics-service.labels" . | nindent 4 }}
  annotations:
    {{- include "opsera-deployment-analytics-service.annotations" . | nindent 4 }}

spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opsera-deployment-analytics-service.selectorLabels" . | nindent 4 }}
