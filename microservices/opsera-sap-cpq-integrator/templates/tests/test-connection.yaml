apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "opsera-sap-cpq-integrator.fullname" . }}-test-connection"
  labels:
    {{- include "opsera-sap-cpq-integrator.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "opsera-sap-cpq-integrator.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
