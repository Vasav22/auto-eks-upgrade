apiVersion: v1
kind: Service
metadata:
  name: {{ include "opsera-validation-service.fullname" . }}
  labels:
    {{- include "opsera-validation-service.labels" . | nindent 4 }}
  annotations:
    service.kubernetes.io/topology-mode: auto
    {{- include "opsera-validation-service.annotations" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opsera-validation-service.selectorLabels" . | nindent 4 }}
