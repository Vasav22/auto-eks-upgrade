apiVersion: v1
kind: Service
metadata:
  name: {{ include "opsera-dockercli-integrator.fullname" . }}
  labels:
    {{- include "opsera-dockercli-integrator.labels" . | nindent 4 }}
  annotations:
    service.kubernetes.io/topology-mode: auto
    {{- include "opsera-dockercli-integrator.annotations" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opsera-dockercli-integrator.selectorLabels" . | nindent 4 }}
