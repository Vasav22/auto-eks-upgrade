apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "opsera-packer-integrator.fullname" . }}-test-connection"
  labels:
    {{- include "opsera-packer-integrator.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "opsera-packer-integrator.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
