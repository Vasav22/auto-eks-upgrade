apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "opsera-vnxt-insights-api.fullname" . }}-test-connection"
  labels:
    {{- include "opsera-vnxt-insights-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "opsera-vnxt-insights-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
