apiVersion: v1
kind: Service
metadata:
  name: {{ include "opsera-sentinel-integrator.fullname" . }}
  labels:
    {{- include "opsera-sentinel-integrator.labels" . | nindent 4 }}
  annotations:
    service.kubernetes.io/topology-mode: auto
    {{- include "opsera-sentinel-integrator.annotations" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opsera-sentinel-integrator.selectorLabels" . | nindent 4 }}
