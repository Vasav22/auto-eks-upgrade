apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "opsera-sentinel-integrator.fullname" . }}-test-connection"
  labels:
    {{- include "opsera-sentinel-integrator.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "opsera-sentinel-integrator.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
