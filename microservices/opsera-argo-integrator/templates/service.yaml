apiVersion: v1
kind: Service
metadata:
  name: {{ include "opsera-argo-integrator.fullname" . }}
  labels:
    {{- include "opsera-argo-integrator.labels" . | nindent 4 }}
  annotations:
    service.kubernetes.io/topology-mode: auto
    {{- include "opsera-argo-integrator.annotations" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opsera-argo-integrator.selectorLabels" . | nindent 4 }}
