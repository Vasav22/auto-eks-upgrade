apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "opsera-pipeline-config.fullname" . }}-test-connection"
  labels:
    {{- include "opsera-pipeline-config.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "opsera-pipeline-config.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
