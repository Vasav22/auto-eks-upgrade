apiVersion: v1
kind: Service
metadata:
  name: {{ include "opsera-pipeline-config.fullname" . }}
  labels:
    {{- include "opsera-pipeline-config.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opsera-pipeline-config.selectorLabels" . | nindent 4 }}
