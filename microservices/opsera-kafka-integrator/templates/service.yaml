apiVersion: v1
kind: Service
metadata:
  name: {{ include "opsera-kafka-integrator.fullname" . }}
  labels:
    {{- include "opsera-kafka-integrator.labels" . | nindent 4 }}
  annotations:
    {{- include "opsera-kafka-integrator.annotations" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opsera-kafka-integrator.selectorLabels" . | nindent 4 }}
