apiVersion: v1
kind: Service
metadata:
  name: {{ include "opsera-twistlock-integrator.fullname" . }}
  labels:
    {{- include "opsera-twistlock-integrator.labels" . | nindent 4 }}
  annotations:
    {{- include "opsera-twistlock-integrator.annotations" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opsera-twistlock-integrator.selectorLabels" . | nindent 4 }}
