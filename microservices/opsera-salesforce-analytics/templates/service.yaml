apiVersion: v1
kind: Service
metadata:
  name: {{ include "opsera-salesforce-analytics.fullname" . }}
  labels:
    {{- include "opsera-salesforce-analytics.labels" . | nindent 4 }}
  annotations:
    service.kubernetes.io/topology-mode: auto
    {{- include "opsera-salesforce-analytics.annotations" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opsera-salesforce-analytics.selectorLabels" . | nindent 4 }}
