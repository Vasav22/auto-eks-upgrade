apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "opsera-salesforce-analytics.fullname" . }}-test-connection"
  labels:
    {{- include "opsera-salesforce-analytics.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "opsera-salesforce-analytics.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
