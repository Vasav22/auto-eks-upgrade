apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "opsera-containerscan-service.fullname" . }}-test-connection"
  labels:
    {{- include "opsera-containerscan-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "opsera-containerscan-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
