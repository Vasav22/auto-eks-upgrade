apiVersion: v1
kind: Service
metadata:
  name: {{ include "opsera-containerscan-service.fullname" . }}
  labels:
    {{- include "opsera-containerscan-service.labels" . | nindent 4 }}
  annotations:
    service.kubernetes.io/topology-mode: auto
    {{- include "opsera-containerscan-service.annotations" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opsera-containerscan-service.selectorLabels" . | nindent 4 }}
