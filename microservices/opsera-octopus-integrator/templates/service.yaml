apiVersion: v1
kind: Service
metadata:
  name: {{ include "opsera-octopus-integrator.fullname" . }}
  labels:
    {{- include "opsera-octopus-integrator.labels" . | nindent 4 }}
  annotations:
    {{- include "opsera-octopus-integrator.annotations" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opsera-octopus-integrator.selectorLabels" . | nindent 4 }}
