apiVersion: v1
kind: Service
metadata:
  name: {{ include "opsera-sfdc-jenkins-integrator.fullname" . }}
  labels:
    {{- include "opsera-sfdc-jenkins-integrator.labels" . | nindent 4 }}
  annotations:
    {{- include "opsera-sfdc-jenkins-integrator.annotations" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opsera-sfdc-jenkins-integrator.selectorLabels" . | nindent 4 }}
