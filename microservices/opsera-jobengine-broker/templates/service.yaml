apiVersion: v1
kind: Service
metadata:
  name: {{ include "opsera-jobengine-broker.fullname" . }}
  labels:
    {{- include "opsera-jobengine-broker.labels" . | nindent 4 }}
  annotations:
    {{- include "opsera-jobengine-broker.annotations" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opsera-jobengine-broker.selectorLabels" . | nindent 4 }}
