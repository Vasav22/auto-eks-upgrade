resources:
  requests:
    memory: 256Mi

autoscaling:
  enabled: true
  minReplicas: 1
  maxReplicas: 2
  targetMemoryUtilizationPercentage: 240

ingress:
  hosts:
    - host: api.opsera-dev.opsera.io
      paths: [/data-migration-file/]

NODE_ENV: testing
