apiVersion: v1
kind: Service
metadata:
  name: {{ include "opsera-node-tokens-service.fullname" . }}
  labels:
    {{- include "opsera-node-tokens-service.labels" . | nindent 4 }}
  annotations:
    {{- include "opsera-node-tokens-service.annotations" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opsera-node-tokens-service.selectorLabels" . | nindent 4 }}
