apiVersion: v1
kind: Service
metadata:
  name: {{ include "opsera-buildkite-integrator.fullname" . }}
  labels:
    {{- include "opsera-buildkite-integrator.labels" . | nindent 4 }}
  annotations:
    {{- include "opsera-buildkite-integrator.annotations" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opsera-buildkite-integrator.selectorLabels" . | nindent 4 }}
