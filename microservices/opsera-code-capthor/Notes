
helm install opsera-code-capthor . \
    -n microservices \
    --set DOCKER_ENV=kubernetes \
    --set image.tag=kube-dev 

helm upgrade opsera-code-capthor . \
    -n microservices \
    --set DOCKER_ENV=kubernetes \
    --set image.tag=kube-dev 


helm uninstall opsera-code-capthor . \
    -n microservices 



#test


helm install opsera-code-capthor . \
    -n microservices \
    --set DOCKER_ENV=kubernetes \
    --set image.tag=kube-test 

helm upgrade opsera-code-capthor . \
    -n microservices \
    --set DOCKER_ENV=kubernetes \
    --set image.tag=kube-test 



helm uninstall opsera-code-capthor . \
    -n microservices 

#production

helm install opsera-code-capthor . \
    -n microservices \
    --set DOCKER_ENV=kubernetes \
    --set image.tag=kube-prod 

helm upgrade opsera-code-capthor . \
    -n microservices \
    --set DOCKER_ENV=kubernetes \
    --set image.tag=kube-prod 


helm uninstall opsera-code-capthor . \
    -n microservices 

<<<<<<< HEAD
#production

helm install opsera-code-capthor . \
    -n microservices \
    --set DOCKER_ENV=kubernetes \
    --set image.tag=kube-prod 

helm upgrade opsera-code-capthor . \
    -n microservices \
    --set DOCKER_ENV=kubernetes \
    --set image.tag=kube-prod 


helm uninstall opsera-code-capthor . \
    -n microservices 
    
=======
>>>>>>> master-dev
#honeywell-sandbox

helm install opsera-code-capthor . \
    -n microservices \
    --set DOCKER_ENV=kubernetes \
    --set image.tag=honeywell-sandbox

helm upgrade opsera-code-capthor . \
    -n microservices \
    --set DOCKER_ENV=kubernetes \
    --set image.tag=khoneywell-sandbox 


helm uninstall opsera-code-capthor . \
    -n microservices 



#kubectl 
kubectl -n microservices get po -l app.kubernetes.io/name=opsera-code-capthor