apiVersion: v1
kind: Service
metadata:
  name: {{ include "opsera-code-capthor.fullname" . }}
  labels:
    {{- include "opsera-code-capthor.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opsera-code-capthor.selectorLabels" . | nindent 4 }}
