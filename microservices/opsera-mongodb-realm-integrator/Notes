#dev
helm -n microservices install --set DOCKER_ENV=kubernetes --set image.tag=kube-dev opsera-mongodb-realm-integrator .

#test
helm -n microservices install --set DOCKER_ENV=kubernetes --set image.tag=kube-test opsera-mongodb-realm-integrator .

#prod
helm -n microservices install --set DOCKER_ENV=kubernetes --set image.tag=kube-prod opsera-mongodb-realm-integrator .


kubectl -n microservices get po -l app.kubernetes.io/name=opsera-mongodb-realm-integrator