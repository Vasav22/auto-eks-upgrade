apiVersion: v1
kind: Service
metadata:
  name: {{ include "opsera-mongodb-realm-integrator.fullname" . }}
  labels:
    {{- include "opsera-mongodb-realm-integrator.labels" . | nindent 4 }}
  annotations:
    service.kubernetes.io/topology-mode: auto
    {{- include "opsera-mongodb-realm-integrator.annotations" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opsera-mongodb-realm-integrator.selectorLabels" . | nindent 4 }}
