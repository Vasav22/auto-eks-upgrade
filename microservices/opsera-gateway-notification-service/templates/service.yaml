apiVersion: v1
kind: Service
metadata:
  name: {{ include "opsera-gateway-notification-service.fullname" . }}
  labels:
    {{- include "opsera-gateway-notification-service.labels" . | nindent 4 }}
  annotations:
    {{- include "opsera-gateway-notification-service.annotations" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opsera-gateway-notification-service.selectorLabels" . | nindent 4 }}
