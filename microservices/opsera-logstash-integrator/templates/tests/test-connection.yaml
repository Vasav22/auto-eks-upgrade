apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "opsera-logstash-integrator.fullname" . }}-test-connection"
  labels:
    {{- include "opsera-logstash-integrator.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "opsera-logstash-integrator.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
