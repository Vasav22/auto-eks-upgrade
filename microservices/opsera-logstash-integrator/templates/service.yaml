apiVersion: v1
kind: Service
metadata:
  name: {{ include "opsera-logstash-integrator.fullname" . }}
  labels:
    {{- include "opsera-logstash-integrator.labels" . | nindent 4 }}
  annotations:
    {{- include "opsera-logstash-integrator.annotations" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opsera-logstash-integrator.selectorLabels" . | nindent 4 }}
