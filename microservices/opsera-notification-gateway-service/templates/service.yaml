apiVersion: v1
kind: Service
metadata:
  name: {{ include "opsera-notification-gateway-service.fullname" . }}
  labels:
    {{- include "opsera-notification-gateway-service.labels" . | nindent 4 }}
  annotations:
    service.kubernetes.io/topology-mode: auto
    {{- include "opsera-notification-gateway-service.annotations" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opsera-notification-gateway-service.selectorLabels" . | nindent 4 }}
