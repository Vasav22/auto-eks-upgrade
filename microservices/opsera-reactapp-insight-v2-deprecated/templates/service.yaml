apiVersion: v1
kind: Service
metadata:
  name: {{ include "opsera-reactapp-insight-v2.fullname" . }}
  labels:
    {{- include "opsera-reactapp-insight-v2.labels" . | nindent 4 }}
  annotations:
    service.kubernetes.io/topology-mode: auto
    {{- include "opsera-reactapp-insight-v2.annotations" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opsera-reactapp-insight-v2.selectorLabels" . | nindent 4 }}
