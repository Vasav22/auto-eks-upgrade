apiVersion: v1
kind: Service
metadata:
  name: {{ include "opsera-customer-service.fullname" . }}
  labels:
    {{- include "opsera-customer-service.labels" . | nindent 4 }}
  annotations:
    {{- include "opsera-customer-service.annotations" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opsera-customer-service.selectorLabels" . | nindent 4 }}
