apiVersion: v1
kind: Service
metadata:
  name: {{ include "opsera-log-accumulator.fullname" . }}
  labels:
    {{- include "opsera-log-accumulator.labels" . | nindent 4 }}
  annotations:
    service.kubernetes.io/topology-mode: auto
    {{- include "opsera-log-accumulator.annotations" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opsera-log-accumulator.selectorLabels" . | nindent 4 }}
