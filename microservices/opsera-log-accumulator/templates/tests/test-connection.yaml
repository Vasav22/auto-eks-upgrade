apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "opsera-log-accumulator.fullname" . }}-test-connection"
  labels:
    {{- include "opsera-log-accumulator.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "opsera-log-accumulator.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
