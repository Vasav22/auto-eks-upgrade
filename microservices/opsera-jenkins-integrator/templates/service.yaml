apiVersion: v1
kind: Service
metadata:
  name: {{ include "opsera-jenkins-integrator.fullname" . }}
  labels:
    {{- include "opsera-jenkins-integrator.labels" . | nindent 4 }}
  annotations:
    {{- include "opsera-jenkins-integrator.annotations" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opsera-jenkins-integrator.selectorLabels" . | nindent 4 }}
