apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "opsera-jenkins-integrator.fullname" . }}-test-connection"
  labels:
    {{- include "opsera-jenkins-integrator.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "opsera-jenkins-integrator.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
