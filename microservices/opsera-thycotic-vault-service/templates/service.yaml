apiVersion: v1
kind: Service
metadata:
  name: {{ include "opsera-thycotic-vault-service.fullname" . }}
  labels:
    {{- include "opsera-thycotic-vault-service.labels" . | nindent 4 }}
  annotations:
    service.kubernetes.io/topology-mode: auto
    {{- include "opsera-thycotic-vault-service.annotations" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opsera-thycotic-vault-service.selectorLabels" . | nindent 4 }}
