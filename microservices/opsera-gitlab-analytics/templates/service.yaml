apiVersion: v1
kind: Service
metadata:
  name: {{ include "opsera-gitlab-analytics.fullname" . }}
  labels:
    {{- include "opsera-gitlab-analytics.labels" . | nindent 4 }}
  annotations:
    service.kubernetes.io/topology-mode: auto
    {{- include "opsera-gitlab-analytics.annotations" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opsera-gitlab-analytics.selectorLabels" . | nindent 4 }}
