apiVersion: v1
kind: Service
metadata:
  name: {{ include "opsera-tools-manager.fullname" . }}
  labels:
    {{- include "opsera-tools-manager.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opsera-tools-manager.selectorLabels" . | nindent 4 }}
