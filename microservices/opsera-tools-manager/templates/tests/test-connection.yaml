apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "opsera-tools-manager.fullname" . }}-test-connection"
  labels:
    {{- include "opsera-tools-manager.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "opsera-tools-manager.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
