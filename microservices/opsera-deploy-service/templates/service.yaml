apiVersion: v1
kind: Service
metadata:
  name: {{ include "opsera-deploy-service.fullname" . }}
  labels:
    {{- include "opsera-deploy-service.labels" . | nindent 4 }}
  annotations:
    service.kubernetes.io/topology-mode: auto
    {{- include "opsera-deploy-service.annotations" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opsera-deploy-service.selectorLabels" . | nindent 4 }}
