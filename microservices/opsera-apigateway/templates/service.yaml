apiVersion: v1
kind: Service
metadata:
  name: {{ include "opsera-apigateway.fullname" . }}
  labels:
    {{- include "opsera-apigateway.labels" . | nindent 4 }}
  annotations:
    {{- include "opsera-apigateway.annotations" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opsera-apigateway.selectorLabels" . | nindent 4 }}
