apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "opsera-apigateway.fullname" . }}-test-connection"
  labels:
    {{- include "opsera-apigateway.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "opsera-apigateway.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
