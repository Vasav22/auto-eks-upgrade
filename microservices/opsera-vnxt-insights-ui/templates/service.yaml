apiVersion: v1
kind: Service
metadata:
  name: {{ include "opsera-vnxt-insights-ui.fullname" . }}
  labels:
    {{- include "opsera-vnxt-insights-ui.labels" . | nindent 4 }}
  annotations:
    service.kubernetes.io/topology-mode: auto
    {{- include "opsera-vnxt-insights-ui.annotations" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opsera-vnxt-insights-ui.selectorLabels" . | nindent 4 }}
