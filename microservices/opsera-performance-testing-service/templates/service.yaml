apiVersion: v1
kind: Service
metadata:
  name: {{ include "opsera-performance-testing-service.fullname" . }}
  labels:
    {{- include "opsera-performance-testing-service.labels" . | nindent 4 }}
  annotations:
    {{- include "opsera-performance-testing-service.annotations" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opsera-performance-testing-service.selectorLabels" . | nindent 4 }}
