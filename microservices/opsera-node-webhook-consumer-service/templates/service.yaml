apiVersion: v1
kind: Service
metadata:
  name: {{ include "opsera-node-webhook-consumer-service.fullname" . }}
  labels:
    {{- include "opsera-node-webhook-consumer-service.labels" . | nindent 4 }}
  annotations:
    {{- include "opsera-node-webhook-consumer-service.annotations" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opsera-node-webhook-consumer-service.selectorLabels" . | nindent 4 }}
