apiVersion: v1
kind: Service
metadata:
  name: {{ include "opsera-node-platform-service.fullname" . }}
  labels:
    {{- include "opsera-node-platform-service.labels" . | nindent 4 }}
  annotations:
    {{- include "opsera-node-platform-service.annotations" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opsera-node-platform-service.selectorLabels" . | nindent 4 }}
