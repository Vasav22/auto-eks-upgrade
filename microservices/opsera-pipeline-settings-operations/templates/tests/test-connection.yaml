apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "opsera-pipeline-settings-operations.fullname" . }}-test-connection"
  labels:
    {{- include "opsera-pipeline-settings-operations.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "opsera-pipeline-settings-operations.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
