apiVersion: v1
kind: Service
metadata:
  name: {{ include "opsera-pipeline-settings-operations.fullname" . }}
  labels:
    {{- include "opsera-pipeline-settings-operations.labels" . | nindent 4 }}
  annotations:
    service.kubernetes.io/topology-mode: auto
    {{- include "opsera-pipeline-settings-operations.annotations" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opsera-pipeline-settings-operations.selectorLabels" . | nindent 4 }}
