apiVersion: v1
kind: Service
metadata:
  name: {{ include "opsera-azure-service.fullname" . }}
  labels:
    {{- include "opsera-azure-service.labels" . | nindent 4 }}
  annotations:
    {{- include "opsera-azure-service.annotations" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opsera-azure-service.selectorLabels" . | nindent 4 }}
