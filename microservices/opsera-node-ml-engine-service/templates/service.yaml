apiVersion: v1
kind: Service
metadata:
  name: {{ include "opsera-node-ml-engine.fullname" . }}
  labels:
    {{- include "opsera-node-ml-engine.labels" . | nindent 4 }}
  annotations:
    {{- include "opsera-node-ml-engine.annotations" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opsera-node-ml-engine.selectorLabels" . | nindent 4 }}
