apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "opsera-node-ml-engine.fullname" . }}-test-connection"
  labels:
    {{- include "opsera-node-ml-engine.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "opsera-node-ml-engine.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
