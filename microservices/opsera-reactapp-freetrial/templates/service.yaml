apiVersion: v1
kind: Service
metadata:
  name: {{ include "opsera-reactapp-freetrial.fullname" . }}
  labels:
    {{- include "opsera-reactapp-freetrial.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opsera-reactapp-freetrial.selectorLabels" . | nindent 4 }}
