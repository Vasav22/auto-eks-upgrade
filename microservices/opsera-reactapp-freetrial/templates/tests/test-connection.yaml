apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "opsera-reactapp-freetrial.fullname" . }}-test-connection"
  labels:
    {{- include "opsera-reactapp-freetrial.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "opsera-reactapp-freetrial.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
