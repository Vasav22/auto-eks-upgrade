apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "opsera-reactapp.fullname" . }}-test-connection"
  labels:
    {{- include "opsera-reactapp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "opsera-reactapp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
