apiVersion: v1
kind: Service
metadata:
  name: {{ include "opsera-cloud-service.fullname" . }}
  labels:
    {{- include "opsera-cloud-service.labels" . | nindent 4 }}
  annotations:
    service.kubernetes.io/topology-mode: auto
  {{- include "opsera-cloud-service.annotations" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opsera-cloud-service.selectorLabels" . | nindent 4 }}
