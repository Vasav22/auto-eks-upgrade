apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "opsera-cloud-service.fullname" . }}-test-connection"
  labels:
    {{- include "opsera-cloud-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: 440953937617.dkr.ecr.us-east-2.amazonaws.com/opsera/busybox:1.32
      command: ['wget']
      args: ['{{ include "opsera-cloud-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
