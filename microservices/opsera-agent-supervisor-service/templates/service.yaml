apiVersion: v1
kind: Service
metadata:
  name: {{ include "opsera-agent-supervisor-service.fullname" . }}
  labels:
    {{- include "opsera-agent-supervisor-service.labels" . | nindent 4 }}
  annotations:
    {{- include "opsera-agent-supervisor-service.annotations" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opsera-agent-supervisor-service.selectorLabels" . | nindent 4 }}
