apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "opsera-agent-supervisor-service.fullname" . }}-test-connection"
  labels:
    {{- include "opsera-agent-supervisor-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "opsera-agent-supervisor-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
