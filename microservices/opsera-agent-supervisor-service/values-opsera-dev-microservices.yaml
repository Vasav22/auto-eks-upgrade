resources:
  requests:
    memory: 512Mi

autoscaling:
  enabled: true
  minReplicas: 1
  maxReplicas: 2
  targetMemoryUtilizationPercentage: 240

ingress:
  enabled: true
  hosts:
    - host: agent.opsera-dev.opsera.io

NODE_ENV: testing