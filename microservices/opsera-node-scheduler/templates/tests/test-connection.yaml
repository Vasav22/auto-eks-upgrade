apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "opsera-node-scheduler.fullname" . }}-test-connection"
  labels:
    {{- include "opsera-node-scheduler.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "opsera-node-scheduler.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
