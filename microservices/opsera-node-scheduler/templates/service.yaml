apiVersion: v1
kind: Service
metadata:
  name: {{ include "opsera-node-scheduler.fullname" . }}
  labels:
    {{- include "opsera-node-scheduler.labels" . | nindent 4 }}
  annotations:
    service.kubernetes.io/topology-mode: auto
    {{- include "opsera-node-scheduler.annotations" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opsera-node-scheduler.selectorLabels" . | nindent 4 }}
