apiVersion: v1
kind: Service
metadata:
  name: {{ include "opsera-mongo-integrator.fullname" . }}
  labels:
    {{- include "opsera-mongo-integrator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opsera-mongo-integrator.selectorLabels" . | nindent 4 }}
