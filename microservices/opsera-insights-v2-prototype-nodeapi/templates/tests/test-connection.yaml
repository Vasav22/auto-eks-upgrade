apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "opsera-insights-v2-prototype-nodeapi.fullname" . }}-test-connection"
  labels:
    {{- include "opsera-insights-v2-prototype-nodeapi.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "opsera-insights-v2-prototype-nodeapi.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
