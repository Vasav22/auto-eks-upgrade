apiVersion: v1
kind: Service
metadata:
  name: {{ include "opsera-hashicorp-vault-service.fullname" . }}
  labels:
    {{- include "opsera-hashicorp-vault-service.labels" . | nindent 4 }}
  annotations:
    {{- include "opsera-hashicorp-vault-service.annotations" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opsera-hashicorp-vault-service.selectorLabels" . | nindent 4 }}
