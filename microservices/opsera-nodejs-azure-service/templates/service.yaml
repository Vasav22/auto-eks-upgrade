apiVersion: v1
kind: Service
metadata:
  name: {{ include "opsera-nodejs-azure-service.fullname" . }}
  labels:
    {{- include "opsera-nodejs-azure-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opsera-nodejs-azure-service.selectorLabels" . | nindent 4 }}
