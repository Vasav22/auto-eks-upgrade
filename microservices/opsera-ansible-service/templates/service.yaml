apiVersion: v1
kind: Service
metadata:
  name: {{ include "opsera-ansible-service.fullname" . }}
  labels:
    {{- include "opsera-ansible-service.labels" . | nindent 4 }}
  annotations:
    service.kubernetes.io/topology-mode: auto
    {{- include "opsera-ansible-service.annotations" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opsera-ansible-service.selectorLabels" . | nindent 4 }}
