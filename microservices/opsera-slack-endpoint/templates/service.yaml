apiVersion: v1
kind: Service
metadata:
  name: {{ include "opsera-slack-endpoint.fullname" . }}
  labels:
    {{- include "opsera-slack-endpoint.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opsera-slack-endpoint.selectorLabels" . | nindent 4 }}
