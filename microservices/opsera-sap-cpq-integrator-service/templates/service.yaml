apiVersion: v1
kind: Service
metadata:
  name: {{ include "opsera-sap-cpq-integrator-service.fullname" . }}
  labels:
    {{- include "opsera-sap-cpq-integrator-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opsera-sap-cpq-integrator-service.selectorLabels" . | nindent 4 }}
