apiVersion: v1
kind: Service
metadata:
  name: {{ include "opsera-vault-service.fullname" . }}
  labels:
    {{- include "opsera-vault-service.labels" . | nindent 4 }}
  annotations:
    {{- include "opsera-vault-service.annotations" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opsera-vault-service.selectorLabels" . | nindent 4 }}
