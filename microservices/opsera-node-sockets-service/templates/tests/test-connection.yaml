apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "opsera-node-sockets-service.fullname" . }}-test-connection"
  labels:
    {{- include "opsera-node-sockets-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "opsera-node-sockets-service.fullname" . }}:{{ .Values.service.port }}/readyz']
  restartPolicy: Never
