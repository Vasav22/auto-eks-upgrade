apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "opsera-git-service.fullname" . }}-test-connection"
  labels:
    {{- include "opsera-git-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "opsera-git-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
