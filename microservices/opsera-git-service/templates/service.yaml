apiVersion: v1
kind: Service
metadata:
  name: {{ include "opsera-git-service.fullname" . }}
  labels:
    {{- include "opsera-git-service.labels" . | nindent 4 }}
  annotations:
    service.kubernetes.io/topology-mode: auto
    {{- include "opsera-git-service.annotations" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opsera-git-service.selectorLabels" . | nindent 4 }}
