apiVersion: v1
kind: Service
metadata:
  name: {{ include "opsera-node-pipeline-monitor.fullname" . }}
  labels:
    {{- include "opsera-node-pipeline-monitor.labels" . | nindent 4 }}
  annotations:
    service.kubernetes.io/topology-mode: auto
    {{- include "opsera-node-pipeline-monitor.annotations" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opsera-node-pipeline-monitor.selectorLabels" . | nindent 4 }}
