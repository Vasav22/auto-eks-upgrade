autoscaling:
  enabled: true
  minReplicas: 1
  maxReplicas: 2

#enable ingress for DEV work only, this should not be done for TEST or other platform tiers
ingress:
  enabled: true
  hosts:
    - host: node-pipeline-monitor-service.opsera-dev-private.opsera.io
      paths:
        - path: /
          pathType: ImplementationSpecific

NODE_ENV: testing
