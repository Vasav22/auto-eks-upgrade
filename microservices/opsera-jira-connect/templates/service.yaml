apiVersion: v1
kind: Service
metadata:
  name: {{ include "opsera-jira-connect.fullname" . }}
  labels:
    {{- include "opsera-jira-connect.labels" . | nindent 4 }}
  annotations:
    service.kubernetes.io/topology-mode: auto
    {{- include "opsera-jira-connect.annotations" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opsera-jira-connect.selectorLabels" . | nindent 4 }}
