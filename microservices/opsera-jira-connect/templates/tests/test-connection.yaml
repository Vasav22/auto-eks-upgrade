apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "opsera-jira-connect.fullname" . }}-test-connection"
  labels:
    {{- include "opsera-jira-connect.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "opsera-jira-connect.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
