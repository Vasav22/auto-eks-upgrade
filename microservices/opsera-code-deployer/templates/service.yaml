apiVersion: v1
kind: Service
metadata:
  name: {{ include "opsera-code-deployer.fullname" . }}
  labels:
    {{- include "opsera-code-deployer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opsera-code-deployer.selectorLabels" . | nindent 4 }}
