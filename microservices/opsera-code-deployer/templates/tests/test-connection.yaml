apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "opsera-code-deployer.fullname" . }}-test-connection"
  labels:
    {{- include "opsera-code-deployer.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "opsera-code-deployer.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
