apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "opsera-analytics-pull-intake-service.fullname" . }}-test-connection"
  labels:
    {{- include "opsera-analytics-pull-intake-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "opsera-analytics-pull-intake-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
