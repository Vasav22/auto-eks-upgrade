apiVersion: v1
kind: Service
metadata:
  name: {{ include "opsera-analytics-pull-intake-service.fullname" . }}
  labels:
    {{- include "opsera-analytics-pull-intake-service.labels" . | nindent 4 }}
  annotations:
    service.kubernetes.io/topology-mode: auto
    {{- include "opsera-analytics-pull-intake-service.annotations" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opsera-analytics-pull-intake-service.selectorLabels" . | nindent 4 }}
