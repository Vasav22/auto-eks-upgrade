apiVersion: v1
kind: Service
metadata:
  name: {{ include "opsera-sfdc-integrator.fullname" . }}
  labels:
    {{- include "opsera-sfdc-integrator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opsera-sfdc-integrator.selectorLabels" . | nindent 4 }}
