apiVersion: v1
kind: Service
metadata:
  name: {{ include "opsera-apigee-integrator.fullname" . }}
  labels:
    {{- include "opsera-apigee-integrator.labels" . | nindent 4 }}
  annotations:
    service.kubernetes.io/topology-mode: auto
    {{- include "opsera-apigee-integrator.annotations" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opsera-apigee-integrator.selectorLabels" . | nindent 4 }}
