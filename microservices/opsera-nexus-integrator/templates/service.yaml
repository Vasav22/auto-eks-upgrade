apiVersion: v1
kind: Service
metadata:
  name: {{ include "opsera-nexus-integrator.fullname" . }}
  labels:
    {{- include "opsera-nexus-integrator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opsera-nexus-integrator.selectorLabels" . | nindent 4 }}
