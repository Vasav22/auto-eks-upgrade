apiVersion: v1
kind: Service
metadata:
  name: {{ include "opsera-mail-service.fullname" . }}
  labels:
    {{- include "opsera-mail-service.labels" . | nindent 4 }}
  annotations:
    {{- include "opsera-mail-service.annotations" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opsera-mail-service.selectorLabels" . | nindent 4 }}
