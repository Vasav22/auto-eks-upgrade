helm upgrade --install opsera-design-system-demo . \
     -n microservices-staging \
    --set DOCKER_ENV=kubernetes \
    --set image.tag=kube-staging \
    --set ingress.hosts[0].host=uat.opsera.io \
    --set ingress.hosts[0].paths[0]=/