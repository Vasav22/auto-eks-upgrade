apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "opsera-design-system-demo.fullname" . }}-test-connection"
  labels:
    {{- include "opsera-design-system-demo.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "opsera-design-system-demo.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
