apiVersion: v1
kind: Service
metadata:
  name: {{ include "opsera-etl-analytics.fullname" . }}
  labels:
    {{- include "opsera-etl-analytics.labels" . | nindent 4 }}
  annotations:
    service.kubernetes.io/topology-mode: auto
    {{- include "opsera-etl-analytics.annotations" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opsera-etl-analytics.selectorLabels" . | nindent 4 }}
