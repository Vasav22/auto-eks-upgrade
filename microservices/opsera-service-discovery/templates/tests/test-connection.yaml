apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "opsera-service-discovery.fullname" . }}-test-connection"
  labels:
    {{- include "opsera-service-discovery.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "opsera-service-discovery.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
