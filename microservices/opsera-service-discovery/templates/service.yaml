apiVersion: v1
kind: Service
metadata:
  name: {{ include "opsera-service-discovery.fullname" . }}
  labels:
    {{- include "opsera-service-discovery.labels" . | nindent 4 }}
  annotations:
    service.kubernetes.io/topology-mode: auto
    {{- include "opsera-service-discovery.annotations" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opsera-service-discovery.selectorLabels" . | nindent 4 }}
