apiVersion: v1
kind: Service
metadata:
  name: {{ include "opsera-snaplogic-integrator.fullname" . }}
  labels:
    {{- include "opsera-snaplogic-integrator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opsera-snaplogic-integrator.selectorLabels" . | nindent 4 }}
