apiVersion: v1
kind: Service
metadata:
  name: {{ include "opsera-jira-integrator.fullname" . }}
  labels:
    {{- include "opsera-jira-integrator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opsera-jira-integrator.selectorLabels" . | nindent 4 }}
