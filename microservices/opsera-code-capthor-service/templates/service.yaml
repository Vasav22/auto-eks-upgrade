apiVersion: v1
kind: Service
metadata:
  name: {{ include "opsera-code-capthor-service.fullname" . }}
  labels:
    {{- include "opsera-code-capthor-service.labels" . | nindent 4 }}
  annotations:
    service.kubernetes.io/topology-mode: auto
    {{- include "opsera-code-capthor-service.annotations" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opsera-code-capthor-service.selectorLabels" . | nindent 4 }}
