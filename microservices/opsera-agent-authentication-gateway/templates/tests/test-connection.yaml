apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "opsera-agent-authentication-gateway.fullname" . }}-test-connection"
  labels:
    {{- include "opsera-agent-authentication-gateway.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "opsera-agent-authentication-gateway.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
