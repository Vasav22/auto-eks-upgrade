apiVersion: v1
kind: Service
metadata:
  name: {{ include "opsera-agent-authentication-gateway.fullname" . }}
  labels:
    {{- include "opsera-agent-authentication-gateway.labels" . | nindent 4 }}
  annotations:
    {{- include "opsera-agent-authentication-gateway.annotations" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opsera-agent-authentication-gateway.selectorLabels" . | nindent 4 }}
