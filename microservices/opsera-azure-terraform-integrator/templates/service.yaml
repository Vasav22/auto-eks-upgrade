apiVersion: v1
kind: Service
metadata:
  name: {{ include "azure-terraform-integrator.fullname" . }}
  labels:
    {{- include "azure-terraform-integrator.labels" . | nindent 4 }}
  annotations:
    {{- include "azure-terraform-integrator.annotations" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "azure-terraform-integrator.selectorLabels" . | nindent 4 }}
