apiVersion: v1
kind: Service
metadata:
  name: {{ include "opsera-coverity-integrator.fullname" . }}
  labels:
    {{- include "opsera-coverity-integrator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opsera-coverity-integrator.selectorLabels" . | nindent 4 }}
