apiVersion: v1
kind: Service
metadata:
  name: {{ include "opsera-terrascan-service.fullname" . }}
  labels:
    {{- include "opsera-terrascan-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opsera-terrascan-service.selectorLabels" . | nindent 4 }}
