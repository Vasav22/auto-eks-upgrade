autoscaling:
  enabled: true
  minReplicas: 1
  maxReplicas: 2

ingress:
  hosts:
    - host: aws-marketplace-registration-service.opsera-dev-private.opsera.io
      paths: [/]

NODE_ENV: staging
