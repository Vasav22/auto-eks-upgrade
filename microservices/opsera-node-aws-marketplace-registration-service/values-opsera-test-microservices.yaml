autoscaling:
  enabled: true
  minReplicas: 1
  maxReplicas: 3

ingress:
  hosts:
    - host: aws-marketplace-registration-service.opsera-test-private.opsera.io
      paths: [/]

NODE_ENV: staging
