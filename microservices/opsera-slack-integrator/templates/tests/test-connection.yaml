apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "opsera-slack-integrator.fullname" . }}-test-connection"
  labels:
    {{- include "opsera-slack-integrator.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "opsera-slack-integrator.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
