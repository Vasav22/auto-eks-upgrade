apiVersion: v1
kind: Service
metadata:
  name: {{ include "opsera-slack-integrator.fullname" . }}
  labels:
    {{- include "opsera-slack-integrator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opsera-slack-integrator.selectorLabels" . | nindent 4 }}
