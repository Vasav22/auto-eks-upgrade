apiVersion: v1
kind: Service
metadata:
  name: {{ include "opsera-git-gateway-service.fullname" . }}
  labels:
    {{- include "opsera-git-gateway-service.labels" . | nindent 4 }}
  annotations:
    {{- include "opsera-git-gateway-service.annotations" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opsera-git-gateway-service.selectorLabels" . | nindent 4 }}
