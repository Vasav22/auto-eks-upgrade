apiVersion: v1
kind: Service
metadata:
  name: {{ include "opsera-customer-terraform-integrator.fullname" . }}
  labels:
    {{- include "opsera-customer-terraform-integrator.labels" . | nindent 4 }}
  annotations:
    service.kubernetes.io/topology-mode: auto
    {{- include "opsera-customer-terraform-integrator.annotations" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opsera-customer-terraform-integrator.selectorLabels" . | nindent 4 }}
