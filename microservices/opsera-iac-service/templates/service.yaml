apiVersion: v1
kind: Service
metadata:
  name: {{ include "opsera-iac-service.fullname" . }}
  labels:
    {{- include "opsera-iac-service.labels" . | nindent 4 }}
  annotations:
    service.kubernetes.io/topology-mode: auto
    {{- include "opsera-iac-service.annotations" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opsera-iac-service.selectorLabels" . | nindent 4 }}
