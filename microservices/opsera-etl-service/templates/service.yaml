apiVersion: v1
kind: Service
metadata:
  name: {{ include "opsera-etl-services.fullname" . }}
  labels:
    {{- include "opsera-etl-services.labels" . | nindent 4 }}
  annotations:
    service.kubernetes.io/topology-mode: auto
    {{- include "opsera-etl-services.annotations" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opsera-etl-services.selectorLabels" . | nindent 4 }}
