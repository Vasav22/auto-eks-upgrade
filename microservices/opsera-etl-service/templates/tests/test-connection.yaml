apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "opsera-etl-services.fullname" . }}-test-connection"
  labels:
    {{- include "opsera-etl-services.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: 440953937617.dkr.ecr.us-east-2.amazonaws.com/opsera/busybox:1.32
      command: ['wget']
      args: ['{{ include "opsera-etl-services.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
