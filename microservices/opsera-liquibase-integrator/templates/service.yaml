apiVersion: v1
kind: Service
metadata:
  name: {{ include "opsera-liquibase-integrator.fullname" . }}
  labels:
    {{- include "opsera-liquibase-integrator.labels" . | nindent 4 }}
  annotations:
    service.kubernetes.io/topology-mode: auto
    {{- include "opsera-liquibase-integrator.annotations" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opsera-liquibase-integrator.selectorLabels" . | nindent 4 }}
