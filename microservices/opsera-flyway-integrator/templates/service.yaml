apiVersion: v1
kind: Service
metadata:
  name: {{ include "opsera-flyway-integrator.fullname" . }}
  labels:
    {{- include "opsera-flyway-integrator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opsera-flyway-integrator.selectorLabels" . | nindent 4 }}
