apiVersion: v1
kind: Service
metadata:
  name: {{ include "opsera-migration-service.fullname" . }}
  labels:
    {{- include "opsera-migration-service.labels" . | nindent 4 }}
  annotations:
    service.kubernetes.io/topology-mode: auto
    {{- include "opsera-migration-service.annotations" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opsera-migration-service.selectorLabels" . | nindent 4 }}
