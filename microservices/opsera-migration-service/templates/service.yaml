apiVersion: v1
kind: Service
metadata:
  name: {{ include "opsera-migration-service-service.fullname" . }}
  labels:
    {{- include "opsera-migration-service-service.labels" . | nindent 4 }}
  annotations:
    service.kubernetes.io/topology-mode: auto
    {{- include "opsera-migration-service-service.annotations" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opsera-migration-service-service.selectorLabels" . | nindent 4 }}
