
helm install opsera-migration-service-service . \
    -n microservices \
    --set DOCKER_ENV=kubernetes \
    --set image.tag=kube-dev \
    --set ingress.hosts[0].host=api-integrator-service.opsera-dev.opsera.io \
    --set ingress.hosts[0].paths[0]=/


helm upgrade opsera-migration-service-service . \
    -n microservices \
    --set DOCKER_ENV=kubernetes \
    --set image.tag=kube-dev \
    --set ingress.hosts[0].host=api-integrator-service.opsera-dev.opsera.io \
    --set ingress.hosts[0].paths[0]=/



helm uninstall opsera-migration-service-service . \
    -n microservices 



#test


helm install opsera-migration-service-service . \
    -n microservices \
    --set DOCKER_ENV=kubernetes \
    --set image.tag=kube-test \
    --set ingress.hosts[0].host=api-integrator-service.opsera-test.opsera.io \
    --set ingress.hosts[0].paths[0]=/


helm upgrade opsera-migration-service-service . \
    -n microservices \
    --set DOCKER_ENV=kubernetes \
    --set image.tag=kube-test \
    --set ingress.hosts[0].host=api-integrator-service.opsera-test.opsera.io \
    --set ingress.hosts[0].paths[0]=/



helm uninstall opsera-migration-service-service . \
    -n microservices 

#production

helm install opsera-migration-service-service . \
    -n microservices \
    --set DOCKER_ENV=kubernetes \
    --set image.tag=kube-prod \
    --set ingress.hosts[0].host=api-integrator-service.opsera-prod.opsera.io \
    --set ingress.hosts[0].paths[0]=/


helm upgrade opsera-migration-service-service . \
    -n microservices \
    --set DOCKER_ENV=kubernetes \
    --set image.tag=kube-prod \
    --set ingress.hosts[0].host=api-integrator-service.opsera-prod.opsera.io \
    --set ingress.hosts[0].paths[0]=/



helm uninstall opsera-migration-service-service . \
    -n microservices 



kubectl -n microservices port-forward deployment/opsera-migration-service-service 5007:5007
kubectl -n microservices get deploy | grep -i "opsera-migration-service-service"

