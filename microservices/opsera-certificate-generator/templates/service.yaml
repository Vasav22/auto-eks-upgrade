apiVersion: v1
kind: Service
metadata:
  name: {{ include "opsera-certificate-generator.fullname" . }}
  labels:
    {{- include "opsera-certificate-generator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opsera-certificate-generator.selectorLabels" . | nindent 4 }}
