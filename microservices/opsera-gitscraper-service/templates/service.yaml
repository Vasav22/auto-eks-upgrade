apiVersion: v1
kind: Service
metadata:
  name: {{ include "opsera-gitscraper-service.fullname" . }}
  labels:
    {{- include "opsera-gitscraper-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opsera-gitscraper-service.selectorLabels" . | nindent 4 }}
