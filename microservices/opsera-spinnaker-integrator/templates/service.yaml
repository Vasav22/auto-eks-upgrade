apiVersion: v1
kind: Service
metadata:
  name: {{ include "opsera-spinnaker-integrator.fullname" . }}
  labels:
    {{- include "opsera-spinnaker-integrator.labels" . | nindent 4 }}
  annotations:
    {{- include "opsera-spinnaker-integrator.annotations" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opsera-spinnaker-integrator.selectorLabels" . | nindent 4 }}
