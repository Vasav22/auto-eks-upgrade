apiVersion: v1
kind: Service
metadata:
  name: {{ include "opsera-monitoring-service.fullname" . }}
  labels:
    {{- include "opsera-monitoring-service.labels" . | nindent 4 }}
  annotations:
    {{- include "opsera-monitoring-service.annotations" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opsera-monitoring-service.selectorLabels" . | nindent 4 }}
