apiVersion: v1
kind: Service
metadata:
  name: {{ include "opsera-tools-management-service.fullname" . }}
  labels:
    {{- include "opsera-tools-management-service.labels" . | nindent 4 }}
  annotations:
    {{- include "opsera-tools-management-service.annotations" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opsera-tools-management-service.selectorLabels" . | nindent 4 }}
