apiVersion: v1
kind: Service
metadata:
  name: {{ include "opsera-snyk-integrator.fullname" . }}
  labels:
    {{- include "opsera-snyk-integrator.labels" . | nindent 4 }}
  annotations:
    {{- include "opsera-snyk-integrator.annotations" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opsera-snyk-integrator.selectorLabels" . | nindent 4 }}
