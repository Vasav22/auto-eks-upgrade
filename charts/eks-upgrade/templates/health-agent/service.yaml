{{- if .Values.healthAgent.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.healthAgent.name }}
  namespace: {{ include "eks-upgrade.namespace" . }}
  labels:
    {{- include "eks-upgrade.labels" . | nindent 4 }}
    app.kubernetes.io/component: health-agent
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.healthAgent.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "eks-upgrade.selectorLabels" (dict "name" .Values.healthAgent.name "instance" .Release.Name) | nindent 4 }}
{{- end }}
