apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "opsera-elk-logstash.fullname" . }}-test-connection"
  labels:
    {{- include "opsera-elk-logstash.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "opsera-elk-logstash.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
