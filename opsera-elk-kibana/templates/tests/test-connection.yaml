apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "opsera-elk-kibana.fullname" . }}-test-connection"
  labels:
    {{- include "opsera-elk-kibana.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "opsera-elk-kibana.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
