# Default values for imagepullsecret-patcher.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

.dockerconfigjson: "eyAiYXV0aHMiOiB7ICI0NDA5NTM5Mzc2MTcuZGtyLmVjci51cy1lYXN0LTIuYW1hem9uYXdzLmNvbSI6IHsgImF1dGgiOiAiIiwgInBhc3N3b3JkIjogIiIsICJ1c2VybmFtZSI6ICJ0ZXN0IiB9IH0gfQ=="

replicaCount: 1

image:
  repository: public.ecr.aws/e7u7c9l1/imagepullsecret-patcher
  pullPolicy: IfNotPresent
  # Overrides the image tag whose default is the chart appVersion.
  tag: "v0.14"

imagePullSecrets: []
nameOverride: ""
fullnameOverride: ""

serviceAccount:
  # Specifies whether a service account should be created
  create: true
  # Annotations to add to the service account
  annotations: {}
  # The name of the service account to use.
  # If not set and create is true, a name is generated using the fullname template
  name: ""

podAnnotations: {}

podSecurityContext: {}
  # fsGroup: 2000

securityContext: {}
  # capabilities:
  #   drop:
  #   - ALL
  # readOnlyRootFilesystem: true
  # runAsNonRoot: true
  # runAsUser: 1000

resources:
  requests:
    cpu: 0.1
    memory: 15Mi
  limits:
    cpu: 0.2
    memory: 30Mi

nodeSelector: {}

tolerations: []

affinity: {}
