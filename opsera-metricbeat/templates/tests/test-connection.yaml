apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "opsera-metricbeat.fullname" . }}-test-connection"
  labels:
    {{- include "opsera-metricbeat.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "opsera-metricbeat.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
